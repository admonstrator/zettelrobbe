'use strict';

/**
 * Placeholder for the round-8 provider streaming test: the agent that
 * teaches the four provider services to stream, to report progress and to
 * hand back the partial text of a cut-off answer replaces this file. It
 * passes so the runner stays green until then.
 */

console.log(
  'ai-provider-streaming: placeholder, replaced by the provider work'
);
process.exit(0);
