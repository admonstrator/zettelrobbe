'use strict';

/**
 * Placeholder for the round-9 creation guard test: the agent that teaches
 * document analysis to use an existing tag or correspondent instead of
 * creating a hard match of it replaces this file. It passes so the runner
 * stays green until then.
 */

console.log('entity-creation-guard: placeholder, replaced by the guard work');
process.exit(0);
