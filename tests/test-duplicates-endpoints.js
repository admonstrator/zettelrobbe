/**
 * Test: duplicates-endpoints
 *
 * Contract placeholder from the phase's contract commit. The test name is
 * registered in scripts/run-tests.js up front so the three phase agents only
 * fill files in; the agent owning this area replaces this file with the real
 * suite. It passes on purpose until then.
 */

'use strict';

console.log(
  '⏳  duplicates-endpoints: contract placeholder, replaced by the phase work'
);
process.exit(0);
