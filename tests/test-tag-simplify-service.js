/**
 * Test: tag-simplify-service
 *
 * Placeholder registered by the round-10 contract so the runner knows the
 * name; the agent that owns the area replaces it with the real cases.
 */

'use strict';

console.log(
  'PASS test-tag-simplify-service (placeholder of the round-10 contract)'
);
